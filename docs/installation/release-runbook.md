# New API ??????

????????????????????????????????????????????? fork ????????

- GitHub ??????????? Docker ?????
- Docker Hub ???????? `xiaotubao/new-api`??? Docker Hub namespace ???? GitHub Actions Repository Variables ??? `DOCKERHUB_IMAGE` ???
- ????? Docker Compose ?? `new-api`?`postgres`?`redis`?
- ???????????????????????????????

## 0. AI ????????

??????????????????????Codex?Claude ???? AI ???????????????????????????????????????????

AI ???????????????????

- ??????????????????? CI ????????????? tag?tag ?? `YYYYMMDD-?commit` ????? `20260528-afe5540c`?
- Docker ?????? release Git tag ? GitHub ??????????? GitHub Actions ??????? tag ??????AI ?????? tag ???????????????????
- ?? SSH ??????????????????AI ??????????????????????????????????????????????????????
- ?? SSH ???????????????????????????? Redis ??????????????????????????????
- ???????????????????????????????????????????
- ????????? `docker compose down -v`?`docker volume rm ...`??? `data`?`logs`?`backups` ???????????????????????????????

## 1. ????????

???????? Docker Compose ???

```text
????: /opt/new-api
????: new-api
????: xiaotubao/new-api:latest
????: 0.0.0.0:3000 -> container 3000
????: ??????????? Caddy ????? 3000
???: postgres:15 ??
??: redis:latest ??
????: /opt/new-api/docker-compose.yml
????: /opt/new-api/docker-compose.override.yml
```

???????

```text
????/HTTPS -> Caddy ?????? -> new-api:3000
```

????????????????????????

```text
/opt/new-api/
  docker-compose.yml
  docker-compose.override.yml
  data/
  logs/
  backups/
```

??????????????????????? `.env` ???????????????

## 2. ?????

??? `main` ??????????

```powershell
cd D:\code-projects\new-api
git checkout main
git fetch origin
git pull --ff-only origin main
git checkout -b feature/<scope>-<short-desc>
```

???????

```text
feature/<scope>-<short-desc>   ???
fix/<scope>-<short-desc>       ????
refactor/<scope>-<short-desc>  ??
docs/<scope>-<short-desc>      ??
chore/<scope>-<short-desc>     ??
hotfix/<scope>-<short-desc>    ????
```

??????????

- Go ????? JSON ????? `common/json.go` ???????
- ??????????? SQLite?MySQL?PostgreSQL?
- ?????? Bun?
- ????????????? `pkg/billingexpr/expr.md`?
- ??????????????????????????????
- ?? relay ?? DTO ??????????????? `omitempty`????? `0`?`false` ????

## 3. ????

?????????????????

?????

```powershell
go test ./...
```

???????

```powershell
go test ./controller ./model
```

?????

```powershell
cd web/default
bun run typecheck
bun run build
```

??????????? key?

```powershell
cd web/default
bun run i18n:sync
```

?????? Go?Docker ? Bun????? GitHub Actions ?????????????

## 4. ?????

?????????

```powershell
git status --short
git diff
```

???? Conventional Commits?

```powershell
git add <files>
git commit -m "feat: add xxx"
git push -u origin feature/<scope>-<short-desc>
```

???? Pull Request ??? `main`?PR ??????

- ?????
- ?????
- ?????
- ?????????????relay/provider?????? i18n?
- ??????????????

??????? `main` ??????

```powershell
git checkout main
git pull --ff-only origin main
git log -1 --oneline
```

??????????

```text
afe5540c
```

?????????????? `main` ??????????????? tag?

```text
YYYYMMDD-?commit
```

???

```text
20260528-afe5540c
```

AI ????????????????? tag????????????????? Git tag?tag ??? GitHub ?????? Docker ?????????????????????? tag ???? GitHub Actions?

## 5. ?? Docker ??

?????? GitHub Actions ?????? Docker Hub??????????? release Git tag????? workflow ????????AI ????????? tag ??????

???????

```powershell
git checkout main
git pull --ff-only origin main
git tag YYYYMMDD-?commit
git push origin YYYYMMDD-?commit
```

???

```powershell
git tag 20260528-afe5540c
git push origin 20260528-afe5540c
```

GitHub ?? tag push ???????

```text
Actions
-> Publish Docker image (Multi-arch)
```

???????

```text
Actions
-> Publish Docker image (Multi-arch)
-> Run workflow
```

?????

```text
Branch: main
tag: YYYYMMDD-?commit
```

???

```text
tag: 20260528-afe5540c
```

???????? tag ??????? GitHub ????workflow ???? tag ??????????

Actions ?????????

```text
xiaotubao/new-api:YYYYMMDD-?commit
xiaotubao/new-api:latest
```

???????????

```text
xiaotubao/new-api:YYYYMMDD-?commit-amd64
xiaotubao/new-api:YYYYMMDD-?commit-arm64
xiaotubao/new-api:latest-amd64
xiaotubao/new-api:latest-arm64
```

?? GitHub Actions Repository Variable `DOCKERHUB_IMAGE` ???????????????????

### Docker Hub ??

GitHub Actions ?????? secrets?

```text
DOCKERHUB_USERNAME
DOCKERHUB_TOKEN
```

???????????

```text
DOCKERHUB_IMAGE
```

`DOCKERHUB_USERNAME` ? Docker Hub ????????? `xiaotubao`?

`DOCKERHUB_TOKEN` ? Docker Hub Personal Access Token?????????????????????????

`DOCKERHUB_IMAGE` ??? Docker Hub ???????? `xiaotubao/new-api`??????workflow ???? `xiaotubao/new-api`?

### ??????

`Username and password required`?

```text
GitHub Actions ?? DOCKERHUB_USERNAME ? DOCKERHUB_TOKEN?? token ? push ???
```

`go build ... exit code: 1`?

```text
????????? build record ? Actions ???? Go ?????
```

`bun run build` ???

```text
????????????? web/default ???
```

???????

```text
?? Docker Hub ?????? token ????? Read & Write ???
```

????????????? GitHub Actions ????????? Docker Hub ????????? tag???????????????? SSH ????????

## 6. ?????

?????????????????????

```text
/opt/new-api/backups/release-YYYYMMDDHHMMSS/
```

SSH ????????

```bash
cd /opt/new-api
IMAGE_NAME="xiaotubao/new-api"
TS=$(date +%Y%m%d%H%M%S)
BACKUP_DIR="backups/release-$TS"
mkdir -p "$BACKUP_DIR"
echo "BACKUP_DIR=$BACKUP_DIR"

cp docker-compose.yml "$BACKUP_DIR/docker-compose.yml"
[ -f docker-compose.override.yml ] && cp docker-compose.override.yml "$BACKUP_DIR/docker-compose.override.yml" || true

docker image tag "${IMAGE_NAME}:latest" "${IMAGE_NAME}:rollback-$TS"

docker compose exec -T postgres pg_dump -U root -d new-api > "$BACKUP_DIR/new-api-db.sql"

docker image inspect "${IMAGE_NAME}:rollback-$TS" \
  --format '{{.RepoTags}} {{.Id}}' > "$BACKUP_DIR/rollback-image.txt"

ls -lh "$BACKUP_DIR"
docker images "$IMAGE_NAME" \
  --format 'table {{.Repository}}\t{{.Tag}}\t{{.ID}}\t{{.CreatedSince}}\t{{.Size}}' | head -20
```

???????????????

```text
docker-compose.yml
docker-compose.override.yml
new-api-db.sql
rollback-image.txt
```

???????????

```text
xiaotubao/new-api:rollback-YYYYMMDDHHMMSS
```

## 7. ?????

?? GitHub Actions ?????????????????????

?? SSH ????????????????????AI ??????????????????????????????????AI ????????????????????????????????????????

? `<tag>` ??????? tag??? `20260528-afe5540c`?

```bash
cd /opt/new-api
IMAGE_NAME="xiaotubao/new-api"

docker pull "${IMAGE_NAME}:<tag>"
docker pull "${IMAGE_NAME}:latest"

docker images "$IMAGE_NAME" \
  --format 'table {{.Repository}}\t{{.Tag}}\t{{.ID}}\t{{.CreatedSince}}\t{{.Size}}' | head -20

docker compose up -d new-api
docker compose ps
```

????? GitHub Actions ?????????? `/opt/new-api/docker-compose.yml` ? `new-api` ??? `image:` ? `IMAGE_NAME:latest` ?????????? Docker Hub namespace ????????? `mik-myp-new-api:latest`?????????? GitHub Actions ??? Docker Hub ????

`docker compose up -d new-api` ????? postgres?redis ?? Started ????? Compose ????????????????????? volume?????????????

???????????????????????

```bash
docker compose down -v
docker volume rm ...
rm -rf data logs backups
```

## 8. ?????

??????????????????

```bash
cd /opt/new-api

for i in $(seq 1 20); do
  docker compose ps
  status=$(docker inspect new-api --format '{{.State.Health.Status}}' 2>/dev/null || echo unknown)
  echo "new-api health=$status"
  [ "$status" = "healthy" ] && break
  sleep 3
done

curl -fsS http://127.0.0.1:3000/api/status | head -c 500
echo

docker logs --tail=120 new-api
```

????????????

```text
new-api health=healthy
postgres healthy
redis healthy
/api/status ?? 200
????? New API <tag> started
???? panic?migration error?database error?redis error
?? relay ?????? 200
```

??????????????????

```bash
curl -I https://<domain>
```

## 9. ????

??????????????????????????????????????????

??????????

```bash
IMAGE_NAME="xiaotubao/new-api"
docker images "$IMAGE_NAME" \
  --format 'table {{.Repository}}\t{{.Tag}}\t{{.ID}}\t{{.CreatedSince}}\t{{.Size}}' | grep -E 'rollback|TAG|latest'
```

?????

```bash
cd /opt/new-api
IMAGE_NAME="xiaotubao/new-api"

docker tag "${IMAGE_NAME}:rollback-<backup-ts>" "${IMAGE_NAME}:latest"
docker compose up -d new-api

docker compose ps
curl -fsS http://127.0.0.1:3000/api/status | head -c 500
echo
docker logs --tail=120 new-api
```

???

```bash
IMAGE_NAME="xiaotubao/new-api"
docker tag "${IMAGE_NAME}:rollback-20260528032953" "${IMAGE_NAME}:latest"
docker compose up -d new-api
```

????????????????????

## 10. ?????

???????????????????????????????????????????????????????????

????????????????

```bash
cd /opt/new-api
TS=$(date +%Y%m%d%H%M%S)
docker compose exec -T postgres pg_dump -U root -d new-api > "backups/pre-db-restore-$TS.sql"
```

?????????????????

```bash
docker compose stop new-api
```

?? PostgreSQL dump ???????? dump ??????????????????????????????????????????

?????????????

```bash
docker compose up -d new-api
docker compose ps
curl -fsS http://127.0.0.1:3000/api/status | head -c 500
docker logs --tail=120 new-api
```

???????????????????????

## 11. ????????

??????? 7 ? 14 ???????????????

?????

```bash
cd /opt/new-api
find backups -maxdepth 2 -type f -printf '%TY-%Tm-%Td %TH:%TM %10s %p\n' | sort
```

?????

```bash
IMAGE_NAME="xiaotubao/new-api"
docker images "$IMAGE_NAME"
```

???????

```text
?????????
???? rollback ??
???????? tag
```

??? SQL ????????????????????????????

## 12. ??????

?????

```bash
cd /opt/new-api
docker compose ps
docker inspect new-api --format '{{.State.Health.Status}}'
```

???????

```bash
docker logs --tail=200 new-api
docker logs -f new-api
```

???????

```bash
curl -fsS http://127.0.0.1:3000/api/status | head -c 1000
```

?? Caddy?

```bash
systemctl status caddy --no-pager
journalctl -u caddy -n 100 --no-pager
```

?????? Redis?

```bash
docker compose logs --tail=100 postgres
docker compose logs --tail=100 redis
docker compose exec -T postgres pg_isready -U root -d new-api
docker compose exec -T redis redis-cli -a '<redis-password>' ping
```

????????

```bash
free -h
df -h / /home /var/lib/docker 2>/dev/null || true
docker system df
```

## 13. ??????

????

- `main` ??????????
- GitHub Actions `Publish Docker image (Multi-arch)` ?????
- ??????? tag??? `20260528-afe5540c`?
- ??? `backups/release-<ts>/`?
- ??? compose ???
- ??? PostgreSQL ????
- ??? rollback ?? tag?

????

- ??? `xiaotubao/new-api:<tag>`?
- ??? `xiaotubao/new-api:latest`?
- `latest` ????????
- ??? `docker compose up -d new-api`?

????

- `new-api` ? `healthy`?
- `postgres` ? `healthy`?
- `redis` ? `healthy`?
- `/api/status` ?? 200?
- ???? `New API <tag> started`?
- ?????????
- ????????????????????

?????

- ?????????
- ?? rollback ?? tag?
- ??????????
- ????????????

## 14. ????????

?????????????

```text
????: 2026-05-28
?? tag: 20260528-afe5540c
??: xiaotubao/new-api:20260528-afe5540c
latest image id: a5d47d58f902
????: /opt/new-api/backups/release-20260528032953/
rollback tag: xiaotubao/new-api:rollback-20260528032953
??:
  new-api healthy
  postgres healthy
  redis healthy
  /api/status 200
  New API 20260528-afe5540c started
```
